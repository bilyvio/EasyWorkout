import React from 'react';
import { Text } from 'react-native';

const CustomButton = () => {
  return (
    <button type="button">
      <Text>test</Text>
    </button>
  );
};

export default CustomButton;
